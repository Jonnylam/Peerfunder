
// $('#profile-information').html('asdfsdfasdf')

// $('#new_profile').submit(function() {  
//     var valuesToSubmit = $(this).serialize();
//     $.ajax({
//         type: "POST"
//         url: $(this).attr('action'), //submits it to the given url of the form
//         data: valuesToSubmit,
//         dataType: "JSON" // you want a difference between normal and ajax-calls, and json is standard
//     }).success(function(json){
//         $('#profile-information').append('<%= j render partial: 'users/profileinformation', locals: {user: @user} %>';
//     });
//     return false; // prevents normal behaviour
// });

    $('#new_profile').on('ajax:success', function(event, data, status, xhr) {
		$('#profile-information').html('<%= j render partial: 'users/profileinformation', locals: {user: @user} %>');
	});

    $('#editform').on('ajax:success', function(event, data, status, xhr) {
		$('#profile-information').html('<%= j render partial: 'users/profileinformation', locals: {user: @user} %>');
		$('#editform').html('')

	});

    $('#edit_location_form').on('ajax:success', function(event, data, status, xhr) {
		$('#editlocationinformation').html('Location: <%= @user.location %> ');
		$('#edit_location_form').html('')

	});

	 $('#edit_biography_form').on('ajax:success', function(event, data, status, xhr) {
		$('#editbiographyinformation').html('<%= @user.biography %> ');
		$('#edit_biography_form').html('')

	});

	  $('#edit_experience_form').on('ajax:success', function(event, data, status, xhr) {
		$('#editexperienceinformation').html('<%= @user.experience %> ');
		$('#edit_experience_form').html('')

	});