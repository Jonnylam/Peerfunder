$( "#edit_button" ).click(function() {
  $('#edit_form').html('<%= j render partial: 'users/addtoprofileinformation', locals: {user: @user} %>')
});
//Form for editing usertype


$( "#show_edit_button" ).click(function() {
  $('.edit_button').show();
  $('#cancel_edit_button').show();
  $('#show_edit_button').hide();
});
//displaying all the edit buttons so user can edit profile

$('#cancel_edit_button').click(function(){
  $('.edit_button').hide();
  $('#show_edit_button').show();
  $('#cancel_edit_button').hide();
});
//Cancel to revert back to normal

$("#edit_location_button").click(function() {
  $('#location').html('<%= j render partial: 'users/locationprofileinformation', locals: {user: @user} %>')
});
//when you click the edit location button, a form pops on the page that lets you change location

$("#edit_biography_button").click(function() {
  $('#biography').html('<%= j render partial: 'users/biographyprofileinformation', locals: {user: @user} %>')
});

$("#edit_experience_button").click(function() {
  $('#experience').html('<%= j render partial: 'users/experienceprofileinformation', locals: {user: @user} %>')
});

