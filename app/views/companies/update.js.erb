    $('#edit_company_profile').on('ajax:success', function(event, data, status, xhr) {
		$('#company_profile').html('<%= j render partial: 'companies/companyprofileinformation', locals: {company: @company} %>');
	});
